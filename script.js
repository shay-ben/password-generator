var generateBtn = document.querySelector("#generate");
generateBtn.addEventListener("click". writePassword);

function writePassword() {
  var password = generatePassword();
  var passwordText = document.querySelector("#password");

 passwordText.value = password;
}

function generatePassword() {
 var characters = {
  numbers = "123456789",
  lowerCase = "abcdefghijklmkopqrstuvwxyz",
  upperCase = "ABCDEFGHIJKLMNOPQRSTUVWXYZ",
  special = "!@#$%^&*()_",
  finalPassword = [""]
  endPassword = "",

};
var passwordLength = math.floor( 
   prompt("How long do you want the password to be? Password must be between 8 to 128 characters");

if (number.NAN(passwordLength) && passwordLength >= 8 && passwordLength <=128){
  alert("Must be between 8 and 128 characters!)

 var charNumbers = confirm("Do you want number characters?");
 var charLowercase = confirm("Do you want lowercase characters?");
 var charUppercase = confirm("Do you want uppercase characters?");
 var charspecial = confirm("Do you want special characters?");

 if (
   !charNumbers &&
   !charLowercase &&
   !charUppercase &&
   !charspecial &&

 ){ 
alert ("you have to choose at least one option!")
var continue = confirm('Do you still want to gerate a password?"); 
if 


 }
    
  









 
  return"myc00lpassword"

  }

